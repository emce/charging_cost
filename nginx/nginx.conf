upstream charging_stats {
    server web:9091;
}

server {
    listen 9091;

    location / {
        proxy_pass http://charging_stats;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_redirect off;
    }
    
    location /static/ {
        alias /code/static-files/;
    }
}
